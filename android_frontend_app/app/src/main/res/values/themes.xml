<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Use AppCompat Dark.NoActionBar to avoid resource linking errors; Compose will provide Material3 at runtime -->
    <style name="Theme.StealthGuard" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="android:statusBarColor">@color/md_theme_background</item>
        <item name="android:navigationBarColor">@color/md_theme_background</item>
    </style>
</resources>
